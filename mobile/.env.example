# Remote AI routes through the local Node API. Keep disabled for the baseline build.
EXPO_PUBLIC_REMOTE_AI_ENABLED=false
# Optional override. Leave blank to auto-detect from Expo host (recommended for emulator/local dev).
EXPO_PUBLIC_API_BASE_URL=
