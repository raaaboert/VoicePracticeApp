# Remote AI routes through the Node API. Set true for APK/EAS builds used for simulation quality testing.
EXPO_PUBLIC_REMOTE_AI_ENABLED=true
# Optional override. Leave blank to auto-detect from Expo host (recommended for emulator/local dev).
EXPO_PUBLIC_API_BASE_URL=
